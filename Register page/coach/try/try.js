/* slice takes two parameter the start and the end if we give it the start only it goes to the end and take all of the array */

let name = 'Bisrat';
console.log(name.slice(1,4))

const div = document.querySelector('.box');
console.log(div.className);